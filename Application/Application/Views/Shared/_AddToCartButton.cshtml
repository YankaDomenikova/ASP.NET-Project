@model BookViewModel

@if (Model.Quantity == 0)
{
    <a class="btn disabled">Add to cart</a>
}
else
{
    <a class="btn btn-warning" asp-controller="Cart" asp-action="AddToCart" asp-route-id="@Model.Id">Add to cart</a>
}
